# Copy to .env; never commit real keys.
OPENAI_API_KEY=
# Choose a model available in your API account.
OPENAI_MODEL=
API_PORT=3001
# Optional (AC10): serve the catalog from another HTTP source returning Service[] JSON. Empty = built-in /api/v1/services.
VITE_CATALOG_URL=
